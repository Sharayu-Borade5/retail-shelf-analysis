# Core ML / vision
ultralytics>=8.0.0          # YOLO-World zero-shot detection
open-clip-torch>=2.20.0     # OpenCLIP brand classification
torch>=1.9.0
torchvision>=0.10.0
opencv-python>=4.7.0
numpy>=1.24.0
Pillow>=9.0.0

# OCR (PaddleOCR is primary; EasyOCR is automatic fallback)
# Install PaddlePaddle first for your platform:
#   CPU:  pip install paddlepaddle
#   GPU:  pip install paddlepaddle-gpu
paddlepaddle
paddleocr>=2.7.0

# EasyOCR (fallback — installed alongside or instead of PaddleOCR)
easyocr>=1.7.0

# Utilities
tqdm>=4.65.0
